# Somatic — Autonomous Build: enrich the UI + README, then run the full repo-finishing suite (Grok 4.6)

You are **Grok 4.6**, the sole builder for `Ardynai/somatic`. The local UI shipped (PR #86, `python -m somatic ui`) and its safety rails are verified. But it's a **functional-minimal v1** (self-graded ~3/5, vanilla SPA) and the **README is still a phase-by-phase dev archive**. Your job now, by yourself — no other AI, no hand-offs, self-review + self-merge, surface after surface — has three parts:
1. **Enrich the UI** to real, polished, accessible craft.
2. **Enrich the README** into a real product front page with visuals.
3. **Run the full Locus Evolution Lab repo-finishing suite** and produce its report sections — so this repo is genuinely *finished*, not just working.
**Step 0 of your first commit:** save this charter to `planning/ENRICH-AND-FINISH.md`.
## The rails are non-negotiable — enrichment must not regress a single one
These were verified holding in v1. Re-run the adversarial self-review after your changes and confirm every one still holds:
- Core **`pyproject` `dependencies == []`** (stdlib-only core). Frontend/build deps live only in `ui/package.json`.
- Bridge **binds `127.0.0.1` only**, refuses non-loopback `Host`/`Origin`; **CSP stays `script-src 'self'`** (no CDN, no external fonts/scripts/images, `Cache-Control: no-store`, `nosniff`, `no-referrer`).
- Every API path **reuses the real engine gates** — `require_consent` → `emergency_screen` → framed advisory (via the adapter) / citation-binding / `screen_biosecurity` / honest-null. The UI never reimplements or bypasses a gate, never rewrites advisory text into something more authoritative.
- **No raw sensor egress** (features only; `/api/sensors/live` stays refused). **Presence render-only.** **Right-to-erasure** (`/api/privacy/erase`) stays reachable and complete.
Enrichment is additive craft + docs + finishing reports. If any enrichment step would weaken a rail, that's a hard-stop — do it a different way.
## Part 1 — Enrich the UI (craft pass, every surface)
Take each surface from ~3/5 to genuinely polished, and keep it local-first + strict-CSP (bundle everything into the static assets the stdlib bridge serves; no runtime CDN).
- **Use the design skills for real** (not a token gesture): `C:\AI\design-skills\` — `impeccable`, `taste-skill`, `ui-ux-pro-max-skill`, `open-design`, `stitch-sdk`, `stitch-skills`, `refero_skill` (offline). Run the anti-slop/craft skills over every screen; lean on **astryx** components/patterns as the quality baseline (per `AGENTS.md` "UI standard"). Use **Refero's free/offline guides — never the paid Refero MCP.**
- **Real design system:** a coherent token set (color, type scale, spacing, radius, elevation, motion), consistent components, light + dark, the founder theme (moss-on-white / near-black) refined into a proper palette with AA contrast. Frontend authoring stays in `ui/` (Next.js/Tailwind ok there); the **shipped** output is still local static assets served under `script-src 'self'`.
- **Polish each surface:** onboarding/consent (make the 7-scope model feel clear and calm, not a checklist), the Status dashboard (a real empty-state when everything's OFF, not a blank), Analyze (charts for own-baseline deviation — a local, bundled chart approach, no CDN), Research/Remedy/Parasite (evidence grades + citations as first-class, honest-null rendered with dignity), Ingestion (preview→confirm flow), Sensor roster (clear "sandbox / needs hardware" states), Privacy/Erasure (obvious, reassuring). Empty, loading, and error states everywhere.
- **Motion:** tasteful, subtle, and fully gated by `prefers-reduced-motion`.
- **Accessibility becomes a real audit, not a spot-check.** Wire **axe** (or equivalent) into the `ui/` authoring build/test at dev time (Node is fine there) and fix to **WCAG 2.1 AA**: keyboard to every control, visible focus, skip link + landmarks, 44px targets, screen-reader names/roles, contrast, reduced motion. Keep the Python bridge tests (landmarks/CSP/gates) too. Raise the panel's a11y grade past "usable."
- **Attach before/after screenshots** of every enriched surface to the PR and refresh `docs/ui-screenshots/`.
## Part 2 — Enrich the README + presentation
Apply `locus-evolution-lab/docs/workflows/GITHUB_REPO_PRESENTATION_POLISH_WORKFLOW.md` and `…/README_VISUAL_DIAGRAM_AND_WEBSITE_EXPERIENCE_WORKFLOW.md` (both are **documentation** workflows — no external widgets, tracking badges, hotlinked images, or website runtime deps; commit any visual as a **static local asset**).
- **Move the phase-by-phase archive** (current README body: "Pre-pivot archive", "Phase 0…Phase N") into `docs/HISTORY.md`. Make `README.md` a real product front page in the workflow's recommended order: **What it is → Why it matters → Status → Features → Demo/Screenshots → Quickstart (`python -m somatic ui`) → Architecture → Configuration → Development → Security/Privacy → Roadmap → License.**
- **Lead with the product, not the internals.** The Security/Privacy section is a selling point here — the consent model, local-first, "nothing leaves the machine", right-to-erasure, informational-not-medical framing. Show it off (honestly).
- **Visuals:** at least one clean **architecture diagram** (core engine ↔ stdlib loopback bridge ↔ local UI; where consent/emergency/citation gates sit) plus real UI screenshots. Author diagrams with the workflow's approved tools (drawio-skill / Excalidraw) and commit as static SVG/PNG under `docs/`. No external image hosts.
- **Badge policy:** follow the workflow — only truthful, static/earned badges (e.g. license once chosen, CI); no vanity or dynamic-tracking badges.
- Keep it **honest**: v1 status, offline fixture corpus (not live literature), English-only, LICENSE unset — say so where the structure calls for it.
## Part 3 — Run the full repo-finishing suite (the reusable "finishing a repo" pass)
Run each applicable Locus Evolution Lab finishing workflow, do the **safe in-repo work** it calls for, and **write its required final-report section** into `planning/` (one file per workflow, or a combined `planning/FINISHING-REPORT.md`). These are **planning/quality lenses** — honor Somatic's identity: local-first, stdlib-only core, single-user; classify any layer that means server-side custody / accounts / cloud / hosted infra as **not-applicable** (don't install it).
- **`PRODUCTION_READINESS_CHECKLIST.md`** — the whole-repo 18-layer gate (front-end, API, storage, auth, hosting, cloud, CI/CD, security, rate-limiting, caching, LB, error-tracking, availability, compliance, testing, ops, maintenance, secrets). Classify each with a one-line justification; local-first makes several N/A — say why.
- **`PRELAUNCH_HARDENING_CHECKLIST.md`** — the prelaunch sequence: build/test baseline, **code cleanup + static hardening**, application-security review, DAST *only against the loopback bridge* (it's the sole web surface; note scope), abuse/resilience, observability/audit (in-page, no egress), availability/recovery, secrets/private-data, release decision.
- **`docs/CODE_CLEANUP_AND_HARDENING_STANDARD.md`** (Praxis) — run the required pass order (ruff/mypy clean, dead-code/dup removal, readability) on changed code; produce the **required cleanup report**; respect the safe-fix policy (don't touch byte-pinned fabric vectors or the `phase12_contracts.py` monolith beyond what's authorized).
- **`REPO_BOOTSTRAP_PONYTAIL_AND_READABILITY_WORKFLOW.md`** — minimal-code discipline + human-readability (a human can maintain it without an AI). Note gaps; fix the cheap ones (naming, module docstrings, a `docs/ARCHITECTURE.md` if thin).
- **`FINAL_PHASE_EXPERT_USER_PANEL_AND_FULL_STACK_BLUEPRINT.md`** — **re-run** the expert user panel against the *enriched* product (not the v1 UI), and complete the **premortem + simulated-postmortem** gate. Update `planning/UI-READINESS.md` grades.
- **`POST_LAUNCH_MOBILE_APP_TRACK.md`** and **`ADAPTIVE_PER_USER_UI_EVOLUTION_TRACK.md`** — refresh the assessments; both stay **deferred / not built** (founder-gated), keep the guardrails noted.
- **Skip and say why:** the exotic / cross-repo / other-repo lenses that don't apply — `QUANTUM_ASSISTED_CODE_AND_LAUNCH_OPTIMIZATION`, `CODEX_CROSS_REPO_QUANTUM_CONSIDERATION`, `GLOSSOPETRAE_CODEC_FINAL_REPORT_REQUIREMENT` (different repo), `PER_REPO_MODEL_PROGRAM`, `HARNESS_STACK_SPEC_GIT_OBSIDIAN_WORKFLOW`. `PUBLIC_GIT_GITHUB_REVIEW_AUTOMATION_WORKFLOW` is already in place (`.cursor/`). Do `FABRIC_INTEROP_FINAL_REPORT_REQUIREMENT` only if you actually touch fabric interop.
Don't rabbit-hole: for each workflow, either do the safe in-repo work + write its report section, or record "N/A — <reason>." Log skips honestly.
## How you work
Read the target files + nearest tests first; build only from what exists. Work in tight PRs (a surface's enrichment, the README pass, a finishing-report batch). Keep green each time: `python -m unittest`, `python -m somatic doctor`, ruff clean on changed Python, the `ui/` build + a11y check clean, **core `dependencies == []`**. Do your own adversarial self-review (re-probe every rail above), then merge yourself on green: `gh pr merge <n> --squash --admin --delete-branch`. Fresh clone off `origin/main`; never build in the locked `C:\AI\somatic`.
## Only stop for these (log in `planning/AUTONOMOUS-LOG.md`; keep working other parts meanwhile)
- Anything that would add a dep to the **Python core**, weaken a safety/privacy rail, or bypass a gate to go green — stop. (Frontend/authoring/a11y deps in `ui/package.json` are fine.)
- Irreversible/external/costly: releasing / deploying / **publishing a public URL or website** / npm / app stores, buying a domain, spending money, emailing/posting, deleting user data, force-push. Local `127.0.0.1` is fine; a public surface is a stop.
- Legal/identity with no safe default: the **LICENSE**, or changing the product's identity (server-side custody, accounts, cloud sync). Small choices (palette, chart lib in `ui/`, diagram tool, crisis-line locale) — pick the safe default, note it, continue.
- Genuinely stuck: build red after real fix attempts, or a finishing lens that needs hardware/weights/founder input — write the report section as "blocked/deferred — <reason>" and move on.
## Done
When the UI is enriched (every surface polished + AA-audited), the README is a real product front page with a committed architecture diagram + screenshots, and every applicable finishing workflow has its report section (or a justified N/A) — all self-reviewed, green, rails intact, and merged to `main` — update `planning/AUTONOMOUS-LOG.md` (what shipped, grades before/after, founder-defaults, anything unsure) and stop. Still founder-gated: **the LICENSE, and releasing / deploying / publishing to real users.** Everything up to merge-to-`main` is yours.
